# Conclave 后端依赖（从 pyproject.toml 生成，用于 Docker 多阶段构建的依赖锁定）
# 运行时依赖
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
httpx>=0.27
python-multipart>=0.0.9
openai>=1.0
qdrant-client>=1.7
websockets>=12.0
playwright>=1.40
beautifulsoup4>=4.12

# 开发依赖（测试用）
pytest>=8.0
pytest-asyncio>=0.23
