# Conclave 后端依赖声明（宽约束，用于生成 requirements.lock）
# 生成方式: uv pip compile requirements.txt -o requirements.lock
# 安装方式: pip install -r requirements.lock（生产/CI/测试统一用 .lock）

# ===== 运行时依赖 =====
fastapi>=0.110
uvicorn[standard]>=0.27
uvloop>=0.19; sys_platform != "win32"
pydantic>=2.5
httpx>=0.27
python-multipart>=0.0.9
openai>=1.0
qdrant-client>=1.7
websockets>=13.0
playwright>=1.40
beautifulsoup4>=4.12
pyyaml>=6.0
psutil>=5.9

# 中文分词
jieba>=0.42

# 数据库
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
psycopg2-binary>=2.9
alembic>=1.13

# 缓存 / 队列
redis[hiredis]>=5.0

# 加密（API Key 持久化）
cryptography>=42.0

# ===== 测试 + lint + type check（不分 dev，单一文件） =====
pytest>=8.0
pytest-asyncio>=0.23
pytest-timeout>=2.3
pytest-xdist>=3.5
ruff>=0.5.0
mypy>=2.0
